<section class="profile">
  <form> 
   <p class="input-field">
      <input type="text" name="username" placeholder="Username">
    </p>
      <p class="input-field">
      <input type="submit" value="submit" class="btn-floating btn-large waves-effect waves-light blue">
    </form>
</section>
  <p> e-mail address: {{user.email}}</p>
  <p> username: {{author.username}}</p>
  <p> motto/mantra: </p>

  {{!-- <h2> e-mail address: {{user.email}}</h2> --}}
  {{#if user.intentions}}
    <div class="intentions">
      {{#each user.intentions}}
         {{> 'intention'}}
      {{/each}}
    </div>
  {{else}}
    <p>No posts yet</p>
  {{/if}}
{{#if author.username}}
    <div class="username">
      {{#each user.username}}
         {{> 'username'}}
      {{/each}}
    </div>
  {{else}}
    <p></p>
  {{/if}}
</section>


