<section class="profile">
  <h2>{{user.email}}</h2>
  {{#if user.intentions}}
    <div class="intentions">
      {{#each user.intentions}}
         {{> 'intention'}}
      {{/each}}
    </div>
  {{else}}
    <p>No posts yet</p>
  {{/if}}
</section>
