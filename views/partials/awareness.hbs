<div class="card">
  <div class="card-content">
    <a class="card-title" href="/awareness{{id}}">
      {{ content }}
    </a>
  </div>
  <div class="card-action">
    <a href="/user/{{ author.id }}" class="">{{ author.email }}</a>
    <span class="awareness__date">{{ createdAt }}</span>
    <span class="awareness_comments">{{ comments.length }} Answers</span>
    <form class="" action="/awareness/{{id}}?_method=DELETE" method="POST">
      <button type="submit" name="button">Delete</button>
    </form>
  </div>
</div>

